@page
@model Desafio2_padaria.Pages.Clientes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Cliente[0].Nome)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente[0].Cpf)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Cliente[0].PontosFidelidade)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Cliente) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PontosFidelidade)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ClienteId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ClienteId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ClienteId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
